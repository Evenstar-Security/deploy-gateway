# Deploy Gateway
Deploy the NGINX web gateway for the Evenstar Security AWS environment.

Installation instructions
-------------------------

    sudo dnf install git
    git clone https://github.com/Evenstar-Security/deploy-gateway.git